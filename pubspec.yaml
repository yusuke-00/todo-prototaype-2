name: todo_prototype2_app
description: A new Flutter application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  provider: ^4.3.2+1
  shared_preferences: ^0.5.8
  dynamic_theme: ^1.0.1
  neon: ^0.0.1
  avatar_glow: ^1.2.0
  charts_flutter: ^0.9.0
  fl_chart: ^0.11.1
  flutter_native_splash: ^0.1.9
  color: #000000

  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true